import 'package:flutter/material.dart';

class BoughtPage extends StatelessWidget {
  const BoughtPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.blue,
    );
  }
}
